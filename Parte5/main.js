/*
let numero = 1
numero = numero + 2  numero (1) + 2 = 3

numero /= 2

  Numbers   (Numeros)
  String    (Texto)
  Null      (Nulo)
  Boolean   (Boleano)
  Undefined (Indefinido)

let gameState = 1 // Essa variavel vai indicar em que estado o jogo esta
*/

const Usuario = "Rafael" // Constantes tambem guardam valores mas não podem alterar o seu valor
//Usuario = "Gabriel"
console.log(Usuario)

while (true) {
  Screen.clear()
  // Sistema de cena de exemplo
  
  // Cena 1
  /*
  if (gameState == 0) { // Se a variavel gameState tiver valor de 0 então acionar o bloco de codigo
     Draw.rect(10, 10, 100, 100, Color.new(0, 255, 0)) // Quadrado verde
  }
  else if (gameState == 1) { // Cena - Se a variavel gameState tiver valor de 1 então acionar o bloco de codigo
     Draw.rect(300, 200, 100, 100, Color.new(255, 0, 0)) // Quadrado vermelho
     Draw.rect(10, 20, 100, 100, Color.new(0, 0, 255)) // Quadrado Azul
     Draw.rect(90, 40, 100, 100, Color.new(255, 255, 0)) // Quadrado Amarelo

  }
  */
   Screen.flip()
}
